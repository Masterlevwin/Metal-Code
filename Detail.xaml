<UserControl x:Class="Metal_Code.Detail"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Metal_Code"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="250" HorizontalContentAlignment="Left" VerticalContentAlignment="Top">
    <UserControl.ContextMenu>
        <ContextMenu>
            <MenuItem Header="Удалить" Click="Remove"></MenuItem>
        </ContextMenu>
    </UserControl.ContextMenu>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <TextBox TextWrapping="Wrap" Text="{Binding NameDetail}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" MinWidth="120" MinHeight="20" VerticalAlignment="Center" MaxLength="40" MaxLines="1" Margin="10,0,0,0" ToolTip="Введите наименование детали"/>
        <TextBox Grid.Column="1" TextWrapping="Wrap" Text="{Binding Count}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" MinWidth="40" MinHeight="20" Margin="20,0,0,0" HorizontalAlignment="Left" ToolTip="Введите количество" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
    
