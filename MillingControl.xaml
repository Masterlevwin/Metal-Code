<UserControl x:Class="Metal_Code.MillingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"     
             xmlns:local="clr-namespace:Metal_Code"
             mc:Ignorable="d"
             d:DesignHeight="30" d:DesignWidth="150">
    <UserControl.ContextMenu>
        <ContextMenu>
            <MenuItem Header="Удалить" Click="Remove"/>
        </ContextMenu>
    </UserControl.ContextMenu>

    <StackPanel Orientation="Horizontal" MinHeight="20" VerticalAlignment="Top">
        <TextBlock Text="Ф" FontWeight="Bold" MinWidth="10" Margin="5,0,0,0"/>
        <TextBlock Text="t" Margin="30,0,0,0" MinWidth="10" VerticalAlignment="Center"/>
        <TextBox TextChanged="SetExtraTime" Margin="5,0,0,0" Text="{Binding ExtraTime, RelativeSource={RelativeSource AncestorType={x:Type local:MillingControl}, Mode=FindAncestor}}" MinWidth="30" MaxWidth="30" IsReadOnly="True" ToolTip="Дополнительное время для настройки" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" TextAlignment="Center"/>
        <TextBlock Text="N" Margin="5,0,0,0" MinWidth="10" VerticalAlignment="Center"/>
        <TextBox TextChanged="SetHoles" Margin="5,0,0,0" Text="{Binding Holes, RelativeSource={RelativeSource AncestorType={x:Type local:MillingControl}, Mode=FindAncestor}}" ToolTip="Введите количество отверстий" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" MaxLength="4" MaxLines="1" MinWidth="30" MaxWidth="30"/>

        <Image Source="/Images/question.png" MaxWidth="10" VerticalAlignment="Top" Margin="2,0,0,0">
            <Image.ToolTip>
                <ToolTip Background="Snow" BorderBrush="Red" Padding="5" BorderThickness="1">
                    <StackPanel >
                        <TextBlock Text="Стоимость фрезеровки = общее время * 80"/>
                    </StackPanel>
                </ToolTip>
            </Image.ToolTip>
        </Image>

    </StackPanel>
</UserControl>
