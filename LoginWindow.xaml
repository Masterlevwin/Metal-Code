<Window x:Class="Metal_Code.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="250" Width="350"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Логин:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="LoginText" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Пароль:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <PasswordBox Grid.Row="1" Grid.Column="1" x:Name="PasswordText" Margin="0,0,0,10"/>

        <!-- ⚠️ УБРАЛИ CheckBox "Запомнить меня" -->
        <TextBlock Grid.Row="2" Grid.ColumnSpan="2" 
                   Text="⚠️ Это временная сессия. При следующем запуске приложения вход будет выполнен под владельцем этого ПК."
                   TextWrapping="Wrap" Foreground="Gray" FontSize="11" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Войти" Width="80" Margin="0,0,10,0" Click="Accept_Click" IsDefault="True"/>
            <Button Content="Выход" Width="80" Click="Exit" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>