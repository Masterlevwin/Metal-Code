<UserControl x:Class="Metal_Code.DetailControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             VerticalAlignment="Top">

    <GroupBox x:Name="DetailBox" Margin="5">
        <GroupBox.Header>
            <StackPanel Orientation="Horizontal" Margin="5">
                <TextBox x:Name="Counter" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" IsReadOnly="True" MinWidth="28"/>
                <TextBox ToolTip="Введите наименование детали" Text="{Binding Title}" TextChanged="SetName" x:Name="DetailName" MinWidth="150" MaxWidth="150" MaxLines="1" Margin="5,0,0,0" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                
                <TextBlock Text="N" VerticalAlignment="Top" FontSize="10" Margin="10,0,0,0" MinWidth="8"/>
                <TextBox ToolTip="Введите количество деталей" Text="{Binding Count}" TextChanged="SetCount" x:Name="Count" MaxWidth="40" MinWidth="40" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Margin="5,0,0,0" Foreground="Blue"/>
                
                <Button Margin="25,0,0,0" Click="ShowDetailData" Padding="2" BorderThickness="0" ToolTip="Сделать простой раскрой">
                    <Image Source="Images/nesting.png"/>
                </Button>

                <ComboBox x:Name="MetalDrop" DisplayMemberPath="Name" ToolTip="Выберите материал для ВСЕХ заготовок" SelectionChanged="SetAllMaterial" Width="120" Margin="25,0,0,0"/>
                <CheckBox ToolTip="Добавить к расчету стоимость материала ВСЕХ заготовок" Click="SetAllMetal" VerticalAlignment="Center" IsChecked="True" Margin="5,0,0,0"/>
                <Button Click="Remove" MouseEnter="EnterBorder" MouseLeave="LeaveBorder" Margin="10,0,0,0">
                    <StackPanel Orientation="Horizontal" Margin="2">
                        <TextBlock Text="Х" FontSize="16" FontWeight="Bold" Padding="0" VerticalAlignment="Center"/>
                        <TextBlock Text="Удалить" VerticalAlignment="Center" Margin="5,0,0,0"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </GroupBox.Header>
        
        <StackPanel x:Name="BilletsStack" Margin="0,3,0,0" HorizontalAlignment="Right">
            <Button Click="AddTypeDetail" HorizontalAlignment="Left" Margin="10,5,0,0">
                <StackPanel Orientation="Horizontal">
                    <Image Source="Images/plus.png" Width="12" Height="12"/>
                    <TextBlock Text="Добавить заготовку" VerticalAlignment="Center" Margin="3,0,0,0" Padding="2"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </GroupBox>
</UserControl>
    
